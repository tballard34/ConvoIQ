# =============================================================================
# ConvoIQ Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Frontend Configuration (Exposed to browser - NO SECRETS!)
# -----------------------------------------------------------------------------
VITE_S3_CONVOS_BUCKET_NAME=convoiq-convos-local
VITE_HARPERDB_URL=http://localhost:9926
VITE_ENVIRONMENT=local

# -----------------------------------------------------------------------------
# Backend Configuration (Server-side only - KEEP SECURE!)
# These variables are ONLY used by resources.ts (HarperDB backend)
# They are NEVER exposed to the browser
# -----------------------------------------------------------------------------
S3_ACCESS_KEY_ID=your_aws_access_key_here
S3_SECRET_ACCESS_KEY=your_aws_secret_key_here
AWS_REGION=us-east-1
S3_CONVOS_BUCKET_NAME=convoiq-convos-local

# Backend-only API keys
DEEPGRAM_API_KEY=your_deepgram_api_key_here
OPENROUTER_API_KEY=your_openrouter_api_key_here

# -----------------------------------------------------------------------------
# Deployment Configuration (For HarperDB CLI - npm run deploy)
# -----------------------------------------------------------------------------
CLI_TARGET=your_harper_cloud_url
CLI_TARGET_USERNAME=your_username
CLI_TARGET_PASSWORD=your_password